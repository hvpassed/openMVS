重建目录xxx参考
  xxx #主目录
    ├─dense #部分结果输出 
    │  ├─images #undistort输出，语义分割需要用该文件夹下图片
    │  ├─pro #语义分割结果
    │  ├─sparse 
    │  └─stereo
    │      ├─consistency_graphs
    │      ├─depth_maps
    │      └─normal_maps
    ├─imgRef.txt #图片分割索引
    ├─pointcloud_dense_refined_bin.ply #重建结果
    ├─pointcloud_dense_refined_color_bin.ply #重建结果
    └─images #重建图片位置
使用方式(xxx为工程主目录路径)
1. colmap重建，包括特征提取、特征匹配、稀疏重建和undistort步骤
2. InterfaceCOLMAP --working-folder xxx -i  xxx/dense -o xxx/scene.mvs  --image-folder images
3. DensifyPointCloud    --working-folder xxx \
    --input-file xxx/scene.mvs \
    --fusion-mode 1 \
    --skip-image-ref false
4. 拷贝imgRef.txt和dense/images到internimage工程中执行语义分割，完成后拷贝结果回到dense/pro下
5. DensifyPointCloud     --working-folder xxx \
    --input-file xxx/scene.mvs  \
    --fusion-mode 2 \
    --geometric-iters -1  \
    --skip-image-ref true \
    --segpro-dir xxx/dense/pro/bins、

切换到xxx目录
6. KNNFindFlann 
7. RefineSeg
8. ResetColor
9. （可选）如果有gps（位置）信息可利用colmap model_aligner功能矫正模型坐标